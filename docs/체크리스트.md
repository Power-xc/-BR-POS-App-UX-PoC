# 던킨 매장 관리 앱 — 구현 체크리스트

> 테스트 완료 항목에 `[x]` 체크.
> 버그 발견 시 하단 "버그 수정 내역" 섹션에 기록.

---

## 버그 수정 내역

| 날짜 | 파일 | 증상 | 원인 | 수정 내용 |
|------|------|------|------|-----------|
| 2026-04-02 | `Snackbar.tsx` | 슬라이드 애니메이션 미작동 | `tailwindcss-animate` 미설치로 `animate-in` 클래스 무효 | `globals.css`에 `@keyframes snackbar-in` + `.snackbar-enter` 클래스 직접 정의로 교체 |
| 2026-04-02 | `Snackbar.tsx` | 프로그레스 바 위치 이상 | 부모에 `relative` 누락으로 `absolute` 기준점 없음 | 내부 div에 `relative` 추가 |
| 2026-04-02 | `BottomNav.tsx` | iOS safe area 미적용 | `h-safe-area-inset-bottom` 은 존재하지 않는 Tailwind 클래스 | `style={{ height: "env(safe-area-inset-bottom, 0px)" }}` 인라인으로 교체 |
| 2026-04-02 | `(mobile)/layout.tsx` | AI비서 입력창 고정 안 됨 | `<main>` 의 `overflow-y-auto` 안에서 자식 `h-full` 미작동 | `<main>` → `overflow-hidden min-h-0` 으로 변경, 각 페이지가 자체 스크롤 관리 |
| 2026-04-02 | `page.tsx` / `order/page.tsx` / `review/page.tsx` | 스크롤 안 됨 | 레이아웃 변경으로 부모 overflow 제거됨 | 각 페이지 루트 div에 `h-full overflow-y-auto hide-scrollbar pb-20` 추가 |
| 2026-04-02 | `assistant/page.tsx` | 하단 탭바에 입력창 가려짐 | 탭바 높이(64px) 만큼 여백 없음 | 루트 div에 `pb-16` 추가 |

---

## 모바일 — 공통

- [ ] 앱 진입 시 WebSocket 실시간 알림 연결
- [ ] 긴급 알림 수신 시 홈 탭 배지 업데이트
- [ ] 오프라인 상태 감지 + 토스트 표시
- [ ] 상단 상태바 매장명 / 날씨 / 시각 표시

---

## 탭 1 — 홈

### AI 데일리 브리핑 카드
- [x] NLP 텍스트 요약 표시 (그래프 없음)
- [x] 핵심 수치 3개 (트렌드 아이콘 포함)
- [x] 하단 "발주 승인 →" CTA
- [ ] 실제 API 연동 (`GET /production/alerts`)
- [ ] 로딩 스켈레톤

### 신선도 라이브 보드
- [x] 상품별 현재고 / 잔여시간 / 소진 예상 시각
- [x] 상태별 컬러 바 (urgent/warning/normal)
- [x] 5분 폴링 구조 (현재 mock)
- [ ] 긴급 항목 클릭 → 생산 등록 바텀시트
- [ ] 실제 API 연동

### 우선순위 알림
- [x] 신호등 3단계 (검정/회색/연회색)
- [x] 읽지 않은 알림 목록
- [x] 원터치 액션 버튼
- [ ] 알림 탭별 딥링크 이동
- [ ] 실시간 WebSocket 수신

---

## 탭 2 — AI 발주

### 발주 아이템 카드
- [x] AI 점수 바 (0–100)
- [x] AI 근거 — 점주 언어 표시 ("내일 비 예보 → 실내 수요↑")
- [x] 수량 스테퍼 (점주 수동 수정)
- [x] 전주/전전주 비교 토글
- [ ] 실제 API 연동 (`GET /order/options/{store_id}`)

### 특이사항 카드
- [ ] 이벤트/캠페인 감지 시 특이사항 표시
- [ ] API 연동 (`GET /order/notice/{option_id}`)

### 마감 임박 배너
- [ ] 마감 20분 전 타이머 배너 노출
- [ ] WebSocket `order_deadline` 수신 연동

### 일괄 승인
- [x] 블랙 전폭 CTA (h-14, Fitts's Law)
- [x] 승인 완료 상태
- [x] Undo 스낵바 8초
- [ ] 실제 API 연동 (`POST /order/confirm`)

---

## 탭 3 — AI 비서

### 채팅 인터페이스
- [x] 사용자 / AI 버블 구분
- [x] 텍스트 입력 + 엔터 전송
- [x] 퀵 버튼 3개
- [x] 스크롤 자동 하단 이동
- [x] 입력창 하단 고정
- [ ] 실제 API 연동 (`POST /chat`)
- [ ] 스트리밍 응답 (SSE/청크)
- [ ] AI 로딩 점 애니메이션

### 발주서 초안 카드
- [x] 인라인 발주서 카드 (아이템 목록 + 총액)
- [x] 승인 버튼
- [x] Undo 스낵바 8초
- [ ] 아이템별 인라인 수량 수정
- [ ] API 연동 (`POST /order/confirm`)

### 응답 유형별 렌더링
- [x] 텍스트 버블
- [x] 발주서 초안 카드
- [ ] 매출 비교 인사이트 카드
- [ ] 재고 현황 미니 카드

---

## 탭 4 — 리뷰

### 종합 점수
- [x] X.X / 5.0 점수
- [x] 전체 리뷰 건수
- [x] 긍정/중립/부정 분포 바
- [x] 트렌드 (↑/↓)
- [ ] 실제 API 연동

### 키워드 클러스터
- [x] 긍정 키워드 (초록 태그)
- [x] 개선 필요 키워드 (빨강 태그)
- [ ] 키워드 클릭 → 해당 리뷰 필터링

### 최근 리뷰 목록
- [x] 별점 + 센티먼트 + 날짜 + 본문
- [ ] 배달 채널 출처 표시
- [ ] 무한 스크롤

---

## POS (`/pos`)

### 메뉴 그리드
- [x] 카테고리 탭 4개
- [x] 상품 카드 (이름 / 가격 / 품절)
- [x] 품절 상품 비활성 처리
- [x] 원터치 장바구니 추가
- [ ] 품절 처리 → Undo 스낵바 연동
- [ ] 30분 후 품절 예상 하이라이트

### 우측 패널 탭 [장바구니|대기열] — V3
- [x] 탭 토글 ([장바구니|대기열])
- [x] 탭 배지 (24px, 검정 배경)
- [x] 장바구니: 수량 조절 + 합계 + 결제 CTA
- [x] 대기열: 제조중/픽업대기 그룹 분리
- [x] 상태 전환 원터치 (제조중→픽업완료)
- [x] Undo 스낵바 8초

### 결제
- [x] 카드결제 / 간편결제 버튼
- [x] 결제 완료 Undo 스낵바
- [ ] 결제 완료 → 대기열 자동 추가
- [ ] 분할결제 지원
- [ ] 인터리지 통합결제

---

## 미구현 — 마인드맵 기반 (POC 후 고도화)

### 포스기 추가 기능
- [ ] 시즌/한정 메뉴 탭 신설
- [ ] 메뉴 검색
- [ ] 배달앱 3사 동시 품절 연동 (요기요/쿠팡이츠/해피오더)
- [ ] AI 추천 탭 (시간·날씨 기반)
- [ ] 대기·KDS DID 연동
- [ ] 기기 연결 상태 모니터링

### 모바일 추가 기능
- [ ] 날씨·이벤트 컨텍스트 카드 (홈)
- [ ] BV 공유 리포트 (프로모션 분석)
- [ ] AI 신뢰 구조 — AI 추천 적중률 공개
- [ ] 고객 관리 (단골 식별 / TOP 리스트)
- [ ] 알림 채널 통합 (배달앱 알림 수신)
- [ ] 일·주·월 리포트

### 키오스크 연계 (POC 후)
- [ ] 키오스크 앱 연동
- [ ] 메뉴·재고 자동 동기화

---

## API 연동 현황

| 기능 | BRK API | 상태 |
|------|---------|------|
| 신선도 보드 | `GET /production/alerts` | Mock |
| 생산 등록 | `POST /production/register` | Mock |
| 발주 옵션 | `GET /order/options/{store_id}` | Mock |
| 특이사항 | `GET /order/notice/{option_id}` | 미구현 |
| 발주 확정 | `POST /order/confirm` | Mock |
| AI 비서 채팅 | `POST /chat` | Mock |
| 실시간 알림 | `WebSocket /ws/alerts` | 미연결 |
| 리뷰 분석 | BRK 정의 예정 | Mock |
